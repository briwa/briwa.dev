<!DOCTYPE html>
<html lang="en">
<head>
    {% include "meta.njk" %}
    <style>{% include "styles/post/index.njk" %}</style>
    <title>Posts</title>
<body>
    <main class="content">
        <nav>
            <ul>
            {%- for post in collections.post | reverse | slice(3) -%}
                <li><a href="{{ post[0].url }}" rel="noreferrer" target="_self">{{ post[0].data.title }}</a></li>
            {%- endfor -%}
            </ul>
        </nav>
    </main>
</body>
</html>
